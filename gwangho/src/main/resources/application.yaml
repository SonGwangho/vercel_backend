spring:
  application:
    name: gwangho
  datasource:
    url: ${DATABASE_URL}

server:
  address: 0.0.0.0

app:
  websocket:
    allowed-origins:
      - https://gwangho.vercel.app
      - http://localhost:3000
      - http://localhost:5173
    auth-token: ${WEBSOCKET_AUTH_TOKEN:}
